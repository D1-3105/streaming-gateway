//
// Created by oleg on 04.05.24.
//

#ifndef STREAMINGGATEWAYCROW_CONSTANTS_H
#define STREAMINGGATEWAYCROW_CONSTANTS_H

#include <cstddef>
#include <opencv2/core/hal/interface.h>
#include <cstdlib>
#include <string>

namespace video_constants{
    const size_t int2uchar = (sizeof(int) / sizeof(uchar));
    const size_t size_t2uchar = (sizeof(size_t) / sizeof(uchar));
}


#endif //STREAMINGGATEWAYCROW_CONSTANTS_H
